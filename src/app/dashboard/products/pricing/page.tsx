import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function PricingPage() {
  return (
    <PagePlaceholder
      title="Pricing"
      subtitle="Manage base, client-specific, and promotional pricing"
      section="Products"
    />
  );
}
