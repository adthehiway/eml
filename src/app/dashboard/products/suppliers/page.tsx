import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function SuppliersPage() {
  return (
    <PagePlaceholder
      title="Suppliers"
      subtitle="Manage supplier directory, products, and lead times"
      section="Products"
    />
  );
}
