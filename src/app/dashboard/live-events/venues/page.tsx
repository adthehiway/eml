import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function VenuesPage() {
  return (
    <PagePlaceholder
      title="Venues"
      subtitle="Venue database, contacts, and commission rates"
      section="Live Events"
    />
  );
}
