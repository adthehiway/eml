import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function StaffPage() {
  return (
    <PagePlaceholder
      title="Crew / Staff"
      subtitle="Staff database, scheduling, and payments"
      section="Live Events"
    />
  );
}
