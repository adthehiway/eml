import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function LocationsPage() {
  return (
    <PagePlaceholder
      title="Locations"
      subtitle="Manage stores, warehouses, and venues"
      section="Admin"
    />
  );
}
