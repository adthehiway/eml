import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function POSSystemsPage() {
  return (
    <PagePlaceholder
      title="POS Systems"
      subtitle="Manage Shopify, NOQ, Square, and AtVenu connections"
      section="Admin"
    />
  );
}
