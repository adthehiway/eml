import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function SystemSettingsPage() {
  return (
    <PagePlaceholder
      title="System Settings"
      subtitle="Configure tax rates, currencies, and default margins"
      section="Admin"
    />
  );
}
