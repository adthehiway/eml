import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function CompaniesPage() {
  return (
    <PagePlaceholder
      title="Companies / Clients"
      subtitle="Manage client accounts, tiers, and contracts"
      section="Admin"
    />
  );
}
