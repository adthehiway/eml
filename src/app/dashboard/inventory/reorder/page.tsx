import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function ReorderManagementPage() {
  return (
    <PagePlaceholder
      title="Reorder Management"
      subtitle="Low stock alerts and reorder recommendations"
      section="Inventory"
    />
  );
}
