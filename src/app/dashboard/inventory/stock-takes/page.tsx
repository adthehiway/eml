import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function StockTakesPage() {
  return (
    <PagePlaceholder
      title="Stock Takes"
      subtitle="Schedule and manage stock counts"
      section="Inventory"
    />
  );
}
