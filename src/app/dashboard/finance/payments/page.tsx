import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function PaymentsPage() {
  return (
    <PagePlaceholder
      title="Payments"
      subtitle="Incoming and outgoing payments"
      section="Finance"
    />
  );
}
